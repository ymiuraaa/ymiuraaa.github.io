# The theme to use, expected to live in themes/<name>
theme = "terminus"

# The URL the site will be built for
base_url = "https://ymiuraaa.github.io"

# The site title and description; used in feeds by default.
title = "Yamato Miura"
description = "Demo website for the Terminus theme for Zola"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", a feed is automatically generated.
generate_feeds = true

# The default author for pages
author = "Yamato Miura"

# The taxonomies to be rendered for the site and their configuration of the default languages
# Example:
#     taxonomies = [
#       {name = "tags", feed = true}, # each tag will have its own feed
#       {name = "tags"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [
    { name = "tags", feed = true },
    { name = "categories", feed = true, paginate_by = 5 },
]

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# CSS class to add to external links (comment line below to disable)
external_links_class = "external"

# Whether footnotes are rendered in the GitHub-style (at the bottom, with back references) or plain (in the place, where they are defined)
bottom_footnotes = true

# Whether to set decoding="async" and loading="lazy" for all images
# When turned on, the alt text must be plain text.
# For example, `![xx](...)` is ok but `![*x*x](...)` isn’t ok
lazy_async_image = true

# When set to "true", support for GitHub-style alerts, a.k.a. callouts or admonitions, is enabled in the Markdown parser.
github_alerts = true

[markdown.highlighting]
# Whether to use inline hex colours ("inline") or CSS classes ("class").
# See https://www.getzola.org/documentation/content/syntax-highlighting/ for details.
style = "inline"

# Eyal: Use a slightly patched version of the Monokai theme that plays more nicely with Rust.
# Long term solution is to improve the Rust grammar used by VSCode/Giallo, which is lacking:
# https://github.com/dustypomerleau/rust-syntax
extra_themes = ["themes/terminus/syntaxes/monokai-classic.json"]

# The theme to use for code highlighting.
# See https://github.com/getzola/giallo?tab=readme-ov-file#themes for the list of allowed values.
theme = "monokai-classic"

[extra]
close_responsive_menu_on_resize = true
color_scheme = "synthwave-84"
favicon_emoji = "👨‍💻"
fediverse_creator = "@ebkalderon@hachyderm.io"
feed_icon = true
layout = "center"
show_default_author = false

main_menu = [
    { name = "blog", url = "blog", trailing_slash = true },
    { name = "gallery", url = "gallery", trailing_slash = true },
    { name = "side quests", url = "side-quests", trailing_slash = true },
    { name = "tags", url = "tags", trailing_slash = true },
]

socials = [
    { name = "github", url = "https://github.com/ymiuraaa" },
]

[extra.content_security_policy]
enable = true
allowed_domains = [
    { directive = "base-uri", domains = ["'self'"] },
    { directive = "connect-src", domains = ["'self'", "cloudflareinsights.com"] },
    { directive = "form-action", domains = ["'self'"] },
    { directive = "script-src", domains = ["'self'", "static.cloudflareinsights.com", "'sha512-8DS7rgIrAmghBFwoOTujcf6D9rXvH8xm8JQ1Ja01h9QX8EzXldiszufYa4IFfKdLUKTTrnSFXLDkUEOTrZQ8Qg=='"] },
]

[extra.responsive_images]
widths = [640, 784, 1280, 1920, 2560]
fallback_width = 1280
